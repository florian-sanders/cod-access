import React from 'react';
import PropTypes from 'prop-types';

import './styles.scss';

const Exercice = ({}) => (
  <div>
    <p>Titre exo</p>
    <p>Meilleur score</p>
  </div>
);

export default Exercice;
